# FRIDAY — Advanced AI Voice Assistant
# All packages are free and open-source. No API keys required.
# Install: pip install -r requirements.txt
# For GPU STT: pip install torch --index-url https://download.pytorch.org/whl/cu128

# ── Core voice I/O ──────────────────────────────────────────────────────────
SpeechRecognition>=3.10.0       # Google STT fallback (needs internet)
sounddevice>=0.4.6              # Low-latency mic streaming
numpy>=1.24.0                   # Required for audio processing and VAD

# ── STT: GPU-accelerated local Whisper ──────────────────────────────────────
faster-whisper>=1.0.0           # CTranslate2-based whisper (CUDA + CPU)
# torch (CUDA build) — install separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# torch is needed for Silero VAD. Silero is downloaded automatically via torch.hub.

# ── TTS ─────────────────────────────────────────────────────────────────────
kokoro-onnx                      # Local neural TTS (Kokoro — no API key, no network)
# edge-tts>=6.1.0               # Microsoft Edge TTS (optional fallback)

# ── Skills ───────────────────────────────────────────────────────────────────
ddgs>=9.0                       # DuckDuckGo search (web_search skill)
psutil>=5.9.0                   # System info skill (CPU, RAM, battery)
pyperclip>=1.8.0                # Clipboard read/write skill

# ── Desktop Automation (Phase 4+) ────────────────────────────────────────────
pyautogui>=0.9.54               # Keyboard/mouse automation (type_text, click, etc.)

# ── Optional ─────────────────────────────────────────────────────────────────
# Pillow>=10.0.0                # Screenshots (skill_screenshot)
# webrtcvad>=2.0.10             # Legacy VAD (replaced by Silero VAD)
